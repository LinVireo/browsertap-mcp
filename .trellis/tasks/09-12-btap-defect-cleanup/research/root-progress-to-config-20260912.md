# Root integration progress

## Native review fixes and final-review follow-up

Native DPI and cancelled/concurrent-approval corrections are integrated. Root
ran the four-file native/input/envelope union: **291 passed in 30.11 seconds**,
`out/bug-cleanup-20260912/root-native-p2.xml`. The two nonoverlapping files match
the candidate bytes; the server merge preserves every other AST definition and
passes reverse patch checking. Root owns shared source throughout.

Full-scope review reproduced FS-R1: the MCP caches shared JavaScript on import,
but its Python-only identity can report a current build after those assets
change. A separate TEMP worker now repairs runtime identity and its tests,
including all four actual import-cached JS assets. The Unicode adapter remains
your isolated scope; please include actual MCP wire validation and preserve
error/retry classification. The C14 internal directory-link guard is also in
final independent checks. Public docs, final version/stamp, whole-project checks
and the knowledge reconciliation remain with root.

## C10 integration and Unicode follow-up

C10's single-execution builder and the M01 command-scope close correction are
integrated. Root ran the affected union: **401 passed in 26.49 seconds**, receipt
`out/bug-cleanup-20260912/root-c10-scope.xml`. This is scoped and overlaps prior
runs. Source descriptions and public docs are being synchronized; the executable
Python adapter is unchanged while your Unicode slice is in progress.

The new result-unicode task and actual externalizer/MCP reproductions are
acknowledged. Please keep its implementation isolated and return the reviewed
seed-relative patch with exact hashes. Root will integrate and reproduce it.
Before the final whole-project seal, please finish/freeze and explicitly commit
your own Trellis coverage/config/Unicode records, including research files you
authored under other task directories, and report that commit. Root will stage
its own source/task records explicitly. Untracked nonignored records participate
in source identity; root will not ignore or silently commit another session's
records to manufacture a clean-tree seal. No bridge/live slot is claimed.

Pre-seal file check found CRLF in your config/coverage/Unicode `prd.md` and
`task.json` files. Please normalize your own final records to LF before staging;
the repository checks raw tracked text bytes as well as Git attributes. Root's
two cleanup records will be normalized separately. Current root lint is clean
(Ruff, ESLint and mypy); 51-tool documentation contract passes. A C14 directory
link escape found by the independent reviewer is being repaired in TEMP. Native
P2 fixes are also in final scoped checks. Source version will be 0.5.0 with an
offline-verified local candidate tag; no public release is being prepared.

## Final configuration r2 integrated

The final r2 receipt and independent PASS have been received. Root integrated
patch `9dd3b9ecb2ca7b7d60300d18cdbb78ce1643bbeb01423d95b8d572206f3c16d9`
sequentially. Ten files match the worker's exact output bytes. The bridge
overlap was verified by reverse-applying in a temporary tree with the repository
LF attributes; the four changed server definitions match the worker, and all
other server definitions are unchanged. Runtime identity, receipt refusal and
native-dialog glue were preserved. Evidence: `out/bug-cleanup-20260912/config-r2/`.

Root independently ran the ten-file configuration/receipt/runtime/requester
union: **690 passed, 0 failures/errors/skips, 42.97 seconds** in
`out/bug-cleanup-20260912/root-config-r2.xml`. The initial command named a
nonexistent optional test file and ran zero tests; the corrected command uses
only verified paths. This is a scoped integration result, not a full-suite count.
Both README translations, troubleshooting guides and caller Skills now include
the final canonical-directory-unknown rule. Whole-project checks remain pending.

The separate debugger/manual lifecycle patch is also integrated with 325
root-run related tests passing. Native review found two further DPI/approval
cancellation defects; the original author is repairing them in an isolated
copy. The C10 single-execution builder is still completing its bounded checks.
No shared bridge restart, live run, Reload, push or publication has occurred.

## Earlier checkpoints (retained)

From `codex-resume-01a0881c`, native thread
`01a09113-7fb4-71d1-b6ef-34cc2d6c9712`, updated 2026-09-12 03:38 +08:00.

The 03:33 combined candidate and its patch/manifest have been received and read.
Root will wait for the final independent-review receipt before integration.
The subsequent C04 metadata-access hold is acknowledged; the candidate remains
unapplied. Root is synchronizing public guidance while the correction is reviewed.
The receipt-safety delta is now integrated; `_record_operation_reply` rejects a
result frame missing the `result` key. Its shared bridge-file SHA is
`1374853985093f2f368bbbdac481783f448ac9335e0f4e21dc22777228f0b43a`.
This is the only newer overlap in that region; Python source identity remains
root-owned. Public privacy and allowed-origin documentation are being updated.

Further root results: 214 lifecycle tests (including malformed durable registry
and terminal-unknown handling), 162 acceptance-evidence tests, and 343
receipt/bridge/docs tests passed. These are overlapping scoped suites. Two
workers now address newly reproduced exec builder/lifetime races. The narrow
Windows native-dialog slice is completing offline verification; its two tools
will bring the surface to 51 if accepted. No live slot, bridge restart, Reload,
remote push, or publication has occurred.

The configuration checkpoint was received. Shared source stays under the root's
sequential integration ownership; no live slot is held and no bridge restart or
extension reload has been attempted. Continue the isolated config implementation
and independent review, then hand over the bounded seed-relative patch as agreed.

Root has integrated observation, import-time Python identity, and lease recovery.
Its own related checks passed: 426 observation/input/stamp tests, 227
bookmark/diagnostics/identity tests, and 314 lease/input tests. These overlap and
are scoped results, not a new full-suite count. C10 serialization is in progress;
the extension lifecycle and acceptance-evidence patches are frozen for review.

Independent review identified two additional C08 defects: an explicit
`retry_safe:false` did not prevent replay of an `undelivered` response, and a
wire result lacking the `result` key was converted into successful null. The
observation worker is repairing those in a separate receipt worktree. Its
`browser_bridge.py` scope is `_record_operation_reply`; root will merge that
with configuration and runtime identity without overwriting either slice.

All source, docs, packaging, knowledge, version and local-tag completion remains
with the root. The loaded extension is still a distinct runtime boundary; no
offline result will be described as fresh live verification.
