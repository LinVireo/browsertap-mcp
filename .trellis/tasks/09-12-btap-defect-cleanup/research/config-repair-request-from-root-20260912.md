# Configuration repair allocation

To `codex-coverage-01a09080`: the root accepted your ready receipt. Combined r2 now has 3081 offline passes, no failures/errors/skips, and approximately 99.53% combined coverage. The first run's sole failure was my pytest basetemp being inside the repository; the isolation test was correct and unchanged. Static and tool evidence passed; package/full-install is running. Original code/tests remain frozen through packaging.

Please proceed with the confirmed C01-C04 configuration/diagnostic repairs in an isolated worktree under `D:/coding`, using the same frozen 13 setup + 18 reviewed test manifests as seed. Keep main-tree source read-only until root integration. Preserve per-file before bytes and return only your own patch, not the seed delta. Root owns public docs, versioning, shared live and knowledge reconciliation.

Also take C16 in the same `browser_bridge.py` slice if reproduced: `Session.reconnect()` currently resets `connect_at` on HTTP polling and `_sync_ext_tabs()` recreates `info.connected_at` on snapshots. Separate last activity from actual connection age; a genuinely new transport/lifetime must reset age and ordinary traffic must not. Cover failover selection with a fake clock. M06 empty/malformed diagnosis classification may be resolved alongside your diagnostics audit; do not claim it currently returns healthy.

Allocated product paths: `paths.py`, `cli.py`, `bridge.py` configuration parsing, `browser_bridge.py` token/path/connection-age and malformed-diagnosis handling, the necessary spawn-env section of `server.py`, and focused tests. A separate runtime-identity worker is using its own worktree for `server.py` setup diagnostics, `browser_bridge.py` diagnose response fields and a new import-time identity module. Keep these regions separate and describe any overlap; root will integrate and verify sequentially.

Observation/navigation deadline and physical lease recovery are assigned to separate local workers. Full inventory and other boundaries are in `defect-ledger-20260912.md`. The user is asleep; continue the authorized repair and review steps without waiting for another interactive approval. No shared live changes or remote push/publication.
