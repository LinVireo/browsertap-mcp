# Final Unicode coordination checkpoint

From `codex-resume-01a0881c`, 2026-09-12.

The shared tree now uses version 0.5.0 and extension stamp `2752911b822fab7e`.
Root independently passed 291 native/input/envelope cases, 148 docs/version/
registry cases, and 24 bookmark cases. These scoped runs overlap other evidence.
The native approval/DPI correction and C14 managed-directory guard are integrated.
Full-scope review found an import-cached JavaScript identity gap; a separate TEMP
worker is delivering the three-file package-source v2 correction. Public docs
are being aligned by root. The final full-suite seal has not started.

Please report the Unicode implementation and independent-review state through
the shared research channel. Return the bounded patch and exact before/after
hashes when ready; any unresolved reproduction should come with the handoff so
root can continue it. Shared `server.py` now also has native cancel glue and
updated descriptions/diagnostic text, which must survive the Unicode merge.

A separate bounded worker is checking the same strict-UTF8 failure in
`bookmark_backup.py` and its existing tests. Your scope remains server result
serialization/adapter and the new result-Unicode regression file. Neither task
changes result-file retention policy.

After root's Unicode acknowledgement, please freeze and commit only your owned
Trellis records, including your cross-task research notes, with LF bytes and the
required secret scan. Root will finish its source/docs/records commit and then
seal that final clean tree. No shared bridge or live slot is held; no remote
push or publication is planned.

## Root integration checkpoint, 05:55 +08:00

Root read the frozen `result-unicode/HANDOFF.md` and server delta. Public README,
Skills and MCP descriptions now document the three archive scopes, explicit
inline JSON fallback, preserved verdicts and JSON-encoded diagnostic fields.
The current root documentation/registration/version check passed 78 tests; both
Skills passed common/Codex validation and lint, with nonblocking length warnings.
Your source patch is still waiting for the agreed independent review receipt.

Bookmark Unicode is integrated with 38 root tests passing. A separate helper
confirmed the same failure in Bottle's real WSGI encoding; root independently
reproduced all eight synthetic HTTP cases, without starting listeners. The
bridge-only worker owns `browser_bridge.py` and a new HTTP regression file in
an isolated copy. This does not change your two-file scope.

Please return the final review result and bounded patch/hash when ready. If a
new finding holds it, record the precise reproduction and current candidate so
the shared coordinator can continue the work. After the approved handoff, you
may freeze and commit your explicitly owned records; root has not staged its
source/docs or modified your records. The remaining whole-project gates and
knowledge reconciliation are ready to proceed after sequential integration.
