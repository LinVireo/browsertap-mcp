# Coverage integration independent check — 2026-09-12

**PASS**：18 个文件的完整差异已独立审查；修复 1 处测试断言缺口后，最终同轮实际
**1222 passed / 0 failures / 0 errors / 0 skipped**。没有未修复的审查发现。

审查者：`trellis-check`，由 `codex-coverage-01a09080` 根会话派发。
工作目录：`D:/browsertap-mcp`；解释器：`D:/venvs/agent-browser-mcp/Scripts/python.exe`。
基线：`db11d228880d6c254581746c1c0812b499664183`，审查期间 HEAD 不变。

`check.jsonl` 的全部 4 个引用、PRD、design、implement、项目与本机 AGENTS、
CONTRIBUTING、Python 质量规范和 wait 错误合同均已读取。项目为 single-repo，规范层为
backend/frontend；此次审查覆盖全部 18 个 Python 测试文件，frontend 没有改动。

## Findings (fixed)

- 文件：`tests/test_pending_bridge_operations.py:133`，
  `test_finish_tab_operations_still_releases_capture_for_numeric_tab_id`。
- 问题：加入另一标签页的捕获后，原来的空 ownership 断言变为数量等于 1，不能辨认释放的
  是哪个标签页。隔离进程内把 `release_tab(client, 1)` 故意改为释放 tab 2，原测试仍通过。
- 修复：保留原断言，并通过公开 `prepare()` 行为验证 tab 2 拒绝其他 requester、tab 1
  可以由另一 requester 重新占用。没有修改生产代码或其他测试。
- 验证：相同故障注入被新断言拦住，实际异常为 `DID NOT RAISE CaptureBusyError`；
  正常实现通过。证据位于 `review/capture-assertion-before-fix.json`、
  `review/capture-assertion-after-fix.json` 与 `review/checker-fix.patch`。

## Findings (not fixed)

无。没有需要产品决策、API 修改或诊断 owner 修复的问题。

## Review scope

下列路径均相对项目根目录，审查的是相对上述 HEAD 的完整差异。

| 文件 | 已核对行为 |
| --- | --- |
| `tests/test_bridge_coverage.py` | PID 记录归属、可选清理失败、监听探测关闭与 CLI help |
| `tests/test_browser_bridge_coverage.py` | 请求体/鉴权、WS 归属、会话清理竞态、远端路由、截止时刻回执 |
| `tests/test_cli_coverage.py` | doctor 恢复指引、前台/重启路径和模块入口 |
| `tests/test_command_scope.py` | 非回环 namespace 隔离、非阻塞 POSIX flock、存储失败关闭 descriptor |
| `tests/test_dialog_policy.py` | 对话框处理证据、协议重试、安全模式停止在物理输入之前 |
| `tests/test_downloads.py` | 下载失败证据、默认目录保留、目标链接越界和单次 dispatch |
| `tests/test_execution_failure_contract.py` | 显式目标、共享 deadline、旧路由 CDP fallback、policy 清理预算 |
| `tests/test_file_write_safety.py` | 原始磁盘错误保留、descriptor 关闭、临时文件和原文件保护 |
| `tests/test_input_boundary_contract.py` | 解析与输入 dispatch 的 deadline，以及原默认目标恢复 |
| `tests/test_mcp_tab_concurrency.py` | elicitation 异常、取消边界、锁释放和请求级默认目标 |
| `tests/test_pending_bridge_operations.py` | 标签页代际、生命周期/回执竞态、manual reservation 与捕获归属 |
| `tests/test_physical_input.py` | PID/DPI 探测及明确跳过激活；物理后端为 stub |
| `tests/test_requester_liveness.py` | 独立模块中的 fork hook 与继承 descriptor 清理 |
| `tests/test_server_coverage.py` | 参数拒绝、stdio/启动、文件与 cookie 路由、wait 失败回执保留 |
| `tests/test_silent_operation_expiry.py` | 重复 unknown 观察不延长 reservation |
| `tests/test_simphtml_coverage.py` | HTML 字符预算、解析结果、导航证据与后续读取预算 |
| `tests/test_site_permissions.py` | elicitation 超时、按会话缓存、origin 错误时目标恢复 |
| `tests/test_tab_create_recovery_contract.py` | 创建前 deadline、浏览器歧义、恢复原 tab 与嵌套错误 |

合成输入已沿生产入口核对：状态通过 registry 的 reserve/complete 等接口建立，竞态通过
事件或受控时间安排；socket、进程启动、浏览器和物理输入边界由 fixture 替代。
未增加 coverage omit，也没有改变生产逻辑。原来的 3 个 command_scope 回归完整保留。

失败 wait 的适配已独立核对：`server.py:4503` 的非成功回执路径保留原 receipt，
`.trellis/spec/backend/error-handling.md:157` 明确禁止未知、丢失或失败结果触发再次 dispatch。
因此 failed 参数应断言原 receipt 加 `operation_status=failed`、`retry_safe=False`、
`poll_with=get_execute_js_result`，当前 6 字段精确断言和单次 dispatch/poll 检查符合合同。
此次仅完善已有合同的测试，无需重复写 spec。

## Verification

下列数字均由本审查者实际运行；完整 argv、开始/结束时间和退出码保存在相应 JSON。
产物前缀：`out/coverage-handoff-20260912/review/`。

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| 最终 18 文件同轮 pytest | exit 0；1222 passed，失败/错误/跳过均 0 | `affected-tests-final.json`、`.log`、`.junit.xml` |
| wait 全参数、wait_reservation_contract、3 个 command_scope 回归 | exit 0；31 passed | `focused-tests.json`、`.log`、`.junit.xml` |
| `python -m ruff check tests --no-cache` | exit 0 | `ruff-tests-final.json`、`.log` |
| `python -m mypy src --cache-dir out/coverage-handoff-20260912/review/mypy-cache` | exit 0；15 source files | `mypy-src.json`、`.log` |
| `git diff --check` | exit 0 | `diff-check-final.json`、`.log` |
| 最终修改测试的原始字节 LF 检查 | 18/18 通过 | `changed-tests-lf-final.json` |
| 原始测试与 HEAD、重复 test 名称、最终运行前后哈希 | 全部通过 | `before.json`、`before-final-tests.json`、`after.json` |

31 项定向检查在断言加强前运行，最终 1222 项在加强后运行；两轮数字不相加。
mypy 之后仅修改测试断言；15 个源码文件的当前哈希均与冻结源码匹配，类型结果仍适用。
实现者原先的 1222 项结果保留在 `integration/`，本次最终结果另行产生，未覆盖其证据。

## Frozen handoff

- 最终 18 测试逐文件 SHA-256：`review/tests-final.json`。
- 诊断 owner 的 11 个产品/文档文件：`review/frozen-final.json`，全部匹配其
  `out/setup-diagnostics-20260911/pre-coverage-integration.json`，mtime 也未变化。
- 最终 15 个 Python 源文件：`review/python-source-final.json`；均与上述源码快照一致。
- 审查仅改动一个文件，其最终 SHA-256：
  `tests/test_pending_bridge_operations.py` =
  `2c4ce5ab158b9940025b972b1fbaa40977aaaee25fedcb511965c64043863105`。
- 最终完整测试补丁：`review/integrated-tests-final.patch`；18 文件，2434 insertions / 32 deletions。
- 汇总：`review/report.json`、`review/checks-final.json`。

根代理可能继续更新 Trellis 元数据；这些不作为被冻结的 11 个产品文件。
审查者已停止测试写入。当前结论只证明此次测试整合；共同树的全量覆盖率、打包和安装
检查由诊断 owner 接收 ready 后负责。本审查没有执行全量 coverage/build、live、桥重启、
扩展 Reload、commit/push、发布或 finalizer，也未采用旧候选的覆盖率作为原目录结果。
