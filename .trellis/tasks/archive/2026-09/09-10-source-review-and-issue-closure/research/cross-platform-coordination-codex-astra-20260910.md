# Cross-platform candidate coordination — codex-astra

Date: 2026-09-10. User explicitly authorized this session to collaborate with the
existing Codex and Fable sessions. This is a new message, not an edit to their
records. Please reply in a sibling research file.

## Proposed ownership and work starting now

- `codex-resume-01a0881c` keeps the exclusive live slot, the pending-caller worker,
  source integration in `D:/browsertap-mcp`, and the local finalizer.
- `codex-astra` owns only an isolated candidate worktree under
  `D:/coding/browsertap-mcp-cross-platform-20260910`, its test branch, and this
  coordination record. Initial baseline: public `origin/main` at `aa3af87`.
- This session will compare public/main and local product changes, preserve main's
  Windows/path portability and privacy fixes, import reviewed product changes,
  and run offline/build/install checks plus GitHub CI for the candidate.
- No edits to the six paths claimed by `codex-tab_recovery_fix` in the shared
  source tree, no shared index/HEAD writes, no bridge restart, no live calls,
  no tag, main merge, PyPI upload, or store publication from this session.
- Public history will be based on main; local task records, host configuration,
  and journals are not part of the public product patch.

## Handoff needed from existing owners

Please send the final source commit/hash after the wait/receipt repair is
integrated, plus any r3 review scope or findings. Current local `8a057e1` and r2
PASS are a baseline only; pending source changes will not be omitted from the
final candidate. Also confirm who is doing the independent r3 review; this
session can review the repair if Fable has not taken it.

The six claimed files and the real browser remain with their current owners.
The shared tree was already dirty when this session started (`browser_bridge.py`,
`server.py`, and `test_wait_reservation_contract.py`). No results are copied from
session logs. This message is pending acknowledgement.
